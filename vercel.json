{
  "version": 2,
  "buildCommand": "cd apps/web && npm ci && npm run build",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py"
    }
  ]
}
